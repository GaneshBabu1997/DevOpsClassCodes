name: Java CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - name: Set up JDK 1.8
      uses: actions/setup-java@master
      with:
        java-version: 1.8
    - name: Build with Maven
      run: mvn package --file pom.xml
    - name: git latest
      run: sudo apt-get install git
    - name : git clone
      run: git clone https://github.com/sandesh2027/DevOpsClassCodes/ DevOpsClassCodes2
    - name : copy 
      run : cp /home/runner/work/DevOpsClassCodes/DevOpsClassCodes/target/addressbook.war DevOpsClassCodes2/
    - name : change directory
      run: cd DevOpsClassCodes2
    - name : config
      run: git config --global user.email "sandesh.1rn14ec126@gmail.com" && git config --global user.name "sandesh2027"
    - name : adding remote
      run: git remote add origin2 https://sandesh2027:2c6e85bad8857d93b05bb726f07254c951b8536c@github.com/sandesh2027/DevOpsClassCodes.git
    - name : git add and commit 
      run : git add . && git commit -m "commit msg"
    - name: remote name
      run: git remote -v >> new.txt
    - name : read
      run : cat new.txt
    - name : git push
      run : git push https://GaneshBabu1997:Ganesh%4097@github.com/GaneshBabu1997/DevOpsClassCodes/ HEAD:master
    
  
      
